\documentclass[$if(fontsize)$$fontsize$$else$11pt$endif$$for(classoption)$,$classoption$$endfor$]{$if(documentclass)$$documentclass$$else$article$endif$}

\usepackage{amsmath,amssymb}
\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX,Scale=MatchLowercase}
$if(mainfont)$
\setmainfont{$mainfont$}
$endif$
\usepackage[margin=1in]{geometry}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{float}
\usepackage{xcolor}
\usepackage{booktabs,longtable,array,tabularx}
\usepackage[section]{placeins}
\usepackage{caption}
\usepackage{pdflscape}
\usepackage{csquotes}
\usepackage{xurl}
$if(biblatex)$
\usepackage[
  backend=biber,
  style=$if(biblio-style)$$biblio-style$$else$authoryear$endif$
]{biblatex}
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
$endif$
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue!45!black,
  citecolor=green!35!black,
  urlcolor=blue!55!black,
  pdftitle={$title$},
  pdfauthor={$for(author)$$author$$sep$; $endfor$}
}

$if(natbib)$
\usepackage[$if(natbiboptions)$$natbiboptions$$else$round,authoryear$endif$]{natbib}
$endif$

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.65em}
\setlength{\emergencystretch}{3em}
\setkeys{Gin}{keepaspectratio}
\captionsetup{font=small,labelfont=bf}
\captionsetup[figure]{skip=6pt}
\captionsetup[table]{skip=5pt}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{5}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.08}
\renewcommand{\floatpagefraction}{0.75}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

$if(highlighting-macros)$
$highlighting-macros$
$endif$

$if(numbersections)$
\setcounter{secnumdepth}{3}
$else$
\setcounter{secnumdepth}{0}
$endif$

$for(header-includes)$
$header-includes$
$endfor$

\title{$title$}
\author{$for(author)$$author$$sep$ \and $endfor$}
\date{$if(date)$$date$$else$\today$endif$}

\begin{document}

\maketitle

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

$if(toc)$
\tableofcontents
$endif$

$body$

$if(natbib)$
$if(bibliography)$
\bibliographystyle{$if(biblio-style)$$biblio-style$$else$plainnat$endif$}
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
$endif$
$endif$

$if(biblatex)$
$if(manual-bibliography)$
$else$
\printbibliography[title={References}]
$endif$
$endif$

$for(include-after)$
$include-after$
$endfor$

\end{document}
